{{page-title "renown-management"}}
<h1>Renown Management</h1>

<RenownNav @orgs={{this.model.orgs}} />

<div class="table-responsive">

<table class="table table-striped">
  <thead>
      <th></th>
      {{#each this.model.people.titles as |title|}}
      <th>{{title}}</th>
      {{/each}}
  </thead>

  <tbody>
    {{#each this.model.people.people as |char| }}
    <tr>
      {{#each-in char as |field value| }}
        <td>
          {{#if (eq field 'char')}}
            <CharIconInline @char={{value}} />
          {{else}}
             {{value}}
          {{/if}}
        </td>
        {{/each-in}}
    </tr>
    {{/each}}
  </tbody>

</table>
</div>

<br><br>
<AddRenownChar @chars={{this.model.people.charnames}} @types={{this.model.people.standard_types}} @addRenownEntry={{this.addRenownEntry}}/>
<ResetAllRenown @resetAllRenown={{this.resetAllRenown}}/>

<div align="right">
<button name="addentry" {{action (mut this.addRenownEntry) true}} class="btn btn-secondary">Add Renown Entry</button>
<button name="renownreset" {{action (mut this.resetAllRenown) true}} class="btn btn-secondary">Reset Renown for All</button>
</div>

{{outlet}}

